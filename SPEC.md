# 技术规格说明书

> 版本：1.0.0 | 代号：qmiao | 发布日期：2026-08-02

## 概述

钱喵 AI 财务助手是一个基于三层分离架构的 AI 辅助财务分析系统。引擎层提供领域无关的处理流水线，配置层通过经验包承载行业知识，数据层负责用户数据的隔离与安全。

## 架构规格

### 三层分离

| 层 | 目录 | 归属 | 不可变性 | 依赖方向 |
|----|------|------|----------|----------|
| 引擎层 | `core/` | 平台维护 | 不可变（版本升级时变更） | 不依赖配置层和数据层 |
| 配置层 | `packs/` | 用户拥有 | 可变（用户自由增删改） | 依赖引擎层提供的引用语法和加载机制 |
| 数据层 | `data-buffer/` | 即用即弃 | 可变（每次任务不同） | 不依赖引擎层和配置层 |

### 引擎层模块

| 文件 | 作用 | 行数 |
|------|------|------|
| `SYSTEM.md` | 核心系统指令：身份、三原则、四层经验包、七阶段流水线 | 122 |
| `SYSTEM-FULL.md` | 全量合并版：SYSTEM + PACK_LOADER + RULE_SCANNER + GATE + REF_SYNTAX + REVIEWER | 1074 |
| `PACK_LOADER.md` | 经验包加载器：纯约定扫描、L1 两层加载、触发机制、多包叠加 | 188 |
| `RULE_SCANNER.md` | 规则扫描器：core/context 区分、5 种规则类型、Instinct 召回、evidence_chain | 265 |
| `GATE.md` | 两级 Gate：Gate-1 数据准入（3 项）、Gate-2 产出校验（5 项）、L4 自动记录 | 228 |
| `REF_SYNTAX.md` | 引用语法：8 种引用格式、L1 三层解析、跨包引用、5 种使用场景 | 161 |
| `REVIEWER.md` | 独立审查 agent：预报告门控四问、误报清单、五维度审查 | 95 |
| `VERSION.json` | 版本信息：版本号、变更日志、架构元数据 | 42 |

### 配置层结构

每个经验包的目录约定：

```
packs/{pack-id}/
├── L1-rules/
│   ├── core/              # 核心规则（始终加载，<=10 条，领域无关）
│   │   ├── accounting.yml
│   │   └── checklist.yml
│   └── context/           # 上下文规则（关键词触发，领域特定）
│       ├── anomaly.yml
│       ├── audit.yml
│       └── variance.yml
├── L2-templates/          # 模板（Markdown，含任务流程和产出格式）
├── L3-cases/              # 案例（Markdown，脱敏后）
├── L4-decision-logs/      # 决策记录（Markdown，AI 自动写入）
│   └── instincts/         # 直觉经验卡片（YAML）
└── README.md              # 经验包说明（用于元数据推断）
```

### 数据层规则

- `input/` 和 `output/` 全部 `.gitignore`，永不提交
- `templates/` 可提交（数据模板，非真实数据）
- 数据脱敏由用户全权负责，AI 只做事前提示和事后善意扫描

## 引用语法规格

| 语法 | 指向 | 解析目录 |
|------|------|----------|
| `[L1:core:name]` | L1 核心规则 | `L1-rules/core/` |
| `[L1:context:name]` | L1 上下文规则 | `L1-rules/context/` |
| `[L1:name]` | L1 规则（先 core 再 context） | `L1-rules/core/` → `context/` |
| `[L2:name]` | L2 模板 | `L2-templates/` |
| `[L3:name]` | L3 案例 | `L3-cases/` |
| `[L4:name]` | L4 决策记录 | `L4-decision-logs/` |
| `[L4:instinct:name]` | L4 Instinct | `L4-decision-logs/instincts/` |
| `[REVIEWER]` | 独立审查 agent | `core/REVIEWER.md` |

L1 标识符匹配优先级：文件名 → 规则 id → 模糊匹配。匹配失败输出提示，不中断处理。

## Gate 规格详解

### Gate-1：数据准入（3 项，非阻断）

| 序号 | 检查项 | 行为 |
|------|--------|------|
| 1 | 数据格式确认 | CSV/Excel/文本/JSON 自动检测，格式不符时提示转换 |
| 2 | 脱敏提示 | 一次性提示，用户确认或忽略后继续 |
| 3 | 技能选择 | 输出本次加载规则清单和处理计划 |

Gate-1 输出规则加载清单格式：`core(全部) + context(命中的文件列表)`

### Gate-2：产出校验（5 项，非阻断，两环节）

**第一环节 - 规则扫描（RULE_SCANNER）**：

| 序号 | 检查项 | 机制 |
|------|--------|------|
| 1 | L1 规则符合性 | YAML 规则机械匹配，标注来源 |
| 2 | L2 模板骨架符合性 | `##` 章节提取 + 占位符填充检查 |
| 3 | L3 案例一致性 | 方法论方向对比 |
| 4 | Instinct 召回 | domain + trigger 匹配，confidence 分流 |

**第二环节 - 独立审查（REVIEWER）**：

| 序号 | 检查项 | 机制 |
|------|--------|------|
| 5 | 独立财务审查 | 预报告门控四问 + 误报清单 + 五维度审查 |

两环节独立输出，互不阻断。

### Instinct 置信度分流

| confidence | 处理方式 | 输出标注 |
|------------|----------|----------|
| >= 0.7 | 自动应用，action 嵌入产出 | `[Instinct·Auto]` |
| 0.3 - 0.6 | 仅提示 | `[Instinct·Hint]` |
| < 0.3 | 静默跳过 | 无 |

## 规则类型规格

L1 规则支持 5 种类型：

| 类型 | 语法 | 示例 |
|------|------|------|
| 数值比较型 | `"abs(实际-预算)/预算 > 0.10"` | 预算偏差检测 |
| 文本匹配型 | `"output.contains('银行账号')"` | 敏感信息检测 |
| 勾稽校验型 | `"收入 - 成本 - 费用 == 利润"` | 报表勾稽 |
| 模板引用型 | `"conforms_to([L2:monthly-report])"` | 模板骨架校验 |
| 自定义逻辑型 | `"由用户在经验包中定义"` | 用户扩展 |

## L1 两层加载机制

| 层 | 目录 | 加载时机 | 数量约束 | 规则特点 |
|----|------|----------|----------|----------|
| core | `L1-rules/core/` | 始终加载 | <=10 条 | 领域无关通用规则 |
| context | `L1-rules/context/` | 关键词命中时加载 | 无限制 | 领域特定规则 |

向后兼容：无 `core/` 和 `context/` 子目录时，全量加载 `L1-rules/` 下的 `.yml` 文件。

## 多包叠加规则

| 层 | 叠加规则 |
|----|----------|
| L1 core 规则 | 后加载覆盖先加载（同名 id 后者为准） |
| L1 context 规则 | 各包独立维护关键词，命中后独立加载 |
| L2 模板 | 后加载覆盖先加载（同名文件名后者为准） |
| L3 案例 | 合并，不覆盖 |
| L4 决策记录 | 不叠加，各包独立 |

## L4 决策记录规格

### 决策记录结构

每条 L4 记录包含 7 个章节：基本信息、处理摘要、关键判断、规则应用、Gate 结果、参考案例、证据链。

### evidence_chain 证据链（6 子节）

| 子节 | 内容 |
|------|------|
| 输入数据快照 | 数据来源、数据摘要（脱敏）、Gate-1 校验状态 |
| 规则命中记录 | 每条 L1 规则的命中/未命中状态 |
| AI 推理步骤 | 处理过程中的关键计算和分析步骤 |
| 产出内容摘要 | 产出类型、关键结论、产出位置 |
| 校验结果 | Gate-2 校验结论 |
| 最终确认 | 用户确认状态和修改记录 |

### evidence_chain vs Instinct

| 维度 | evidence_chain | Instinct |
|------|----------------|----------|
| 存储 | L4 决策记录内部章节 | instincts/ 独立 YAML |
| 生成 | 每次任务自动 | 用户纠正时提取或手动创建 |
| 加载 | `[L4:name]` 引用时全量加载 | domain + trigger 匹配后加载 |
| 粒度 | 完整任务级别 | 单条经验卡片 |

## 工具脚本规格

| 工具 | 用法 | 功能 |
|------|------|------|
| `init_pack.py` | `python init_pack.py <pack-id> <pack-name>` | 从 _template 创建新经验包 |
| `validate_pack.py` | `python validate_pack.py <pack_dir>` | 校验经验包结构和规则完整性 |
| `scan_pack.py` | `python scan_pack.py <pack_dir> / --all` | 扫描 prompt injection 风险 |

### 安全扫描风险分级

| 级别 | 检测内容 |
|------|----------|
| Alert | 忽略上文指令、角色劫持、系统指令窃取、规则绕过、API Key 泄露 |
| Warn | 强制指令约束、立即执行指令、隐瞒用户指令、外部 URL 链接 |
| Info | 记忆注入指令、行为覆写指令、角色扮演指令 |

外部 URL 白名单：`.gov.cn`、`.mof.gov.cn`、`.chinatax.gov.cn`、`.cas.org.cn` 等政府部门域名。

## 兼容性

- 最低经验包版本：`>=1.0.0`
- 向后兼容：无 `core/context` 子目录的旧格式 L1 规则自动全量加载
- 平台无关：任何支持系统指令的 AI 平台均可使用
